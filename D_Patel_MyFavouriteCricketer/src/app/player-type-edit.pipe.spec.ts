import { PlayerTypeEditPipe } from './player-type-edit.pipe';

describe('PlayerTypeEditPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerTypeEditPipe();
    expect(pipe).toBeTruthy();
  });
});
